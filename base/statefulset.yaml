# PostgreSQL StatefulSet for order-service.
#
# WHY StatefulSet (not Deployment):
#   - Stable, predictable pod name: order-postgres-0
#   - Stable network identity via headless service
#   - volumeClaimTemplates provisions a dedicated PVC per pod
#   - Ordered startup/shutdown guarantees data consistency

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: order-postgres
  namespace: order-postgres
  labels:
    app.kubernetes.io/name: order-postgres
    app.kubernetes.io/component: database
    app.kubernetes.io/version: "16"
spec:
  serviceName: order-postgres-headless # Harus cocok dengan nama Service Headless
  replicas: 1
  selector:
    matchLabels:
      app: order-postgres
  template:
    metadata:
      labels:
        app: order-postgres
        app.kubernetes.io/name: order-postgres
        app.kubernetes.io/component: database
    spec:
      # ── Security ─────────────────────────────────────────────────────────────
      securityContext:
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        runAsNonRoot: true

      terminationGracePeriodSeconds: 60

      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP

          # ── Credentials from Secret ─────────────────────────────────────────
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: order-postgres-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-postgres-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: order-postgres-secret
                  key: POSTGRES_DB
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata

          # ── Health probes ───────────────────────────────────────────────────
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - $(POSTGRES_USER)
                - -d
                - $(POSTGRES_DB)
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5

          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - $(POSTGRES_USER)
                - -d
                - $(POSTGRES_DB)
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
            successThreshold: 1

          # ── Resources ───────────────────────────────────────────────────────
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          # ── Volume mount ────────────────────────────────────────────────────
          volumeMounts:
            - name: order-postgres-data
              mountPath: /var/lib/postgresql/data

  # ── PVC Template ─────────────────────────────────────────────────────────────
  # Nama PVC akan menjadi: order-postgres-data-order-postgres-0
  volumeClaimTemplates:
    - metadata:
        name: order-postgres-data
        labels:
          app.kubernetes.io/name: order-postgres
          app.kubernetes.io/component: storage
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
