# PostgreSQL credentials for order-service database.
#
# ⚠️  DO NOT commit real values to git.
#     Values below are base64-encoded PLACEHOLDERS.
#
# Generate real values:
#    echo -n "your-actual-password" | base64
#
# Or create imperatively (recommended):
#    kubectl create secret generic order-postgres-secret \
#      --namespace order-postgres \
#      --from-literal=POSTGRES_USER=postgres \
#      --from-literal=POSTGRES_PASSWORD=yourStrongPassword \
#      --from-literal=POSTGRES_DB=order_db

apiVersion: v1
kind: Secret
metadata:
  name: order-postgres-secret
  namespace: order-postgres
  labels:
    app.kubernetes.io/name: order-postgres
    app.kubernetes.io/component: secret
type: Opaque
data:
  # echo -n "postgres" | base64
  POSTGRES_USER: cG9zdGdyZXM=

  # echo -n "yourStrongPassword" | base64
  POSTGRES_PASSWORD: eW91clN0cm9uZ1Bhc3N3b3Jk

  # POSTGRES_DB disesuaikan menjadi order_db
  # echo -n "order_db" | base64
  POSTGRES_DB: b3JkZXJfZGI=
